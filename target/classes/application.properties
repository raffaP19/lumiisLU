# ======== CONFIGURA��O POSTGRESQL ========

# URL de conex�o com o banco de dados
# Host: localhost, Porta: 5432, Nome do banco: postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres

spring.datasource.username=postgres
spring.datasource.password=Zez@21@@

# Driver do PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# ======== CONFIGURA��O JPA / HIBERNATE ========

# Dialeto espec�fico para o PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# O Hibernate ir� gerenciar o schema do banco (criar/atualizar tabelas)
spring.jpa.hibernate.ddl-auto=update

# Comandos SQL executados no console
spring.jpa.show-sql=true